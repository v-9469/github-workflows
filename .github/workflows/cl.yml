name: CI Pipeline Demo            # Workflow name (shows in GitHub UI)

on: [push, pull_request]      # When to run this workflow

jobs:                          # Define jobs
   job1:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Job 1"
  
      
   job2:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Job 2"